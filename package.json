{
  "name": "klaus",
  "version": "1.0.0",
  "description": "API for website Klaus. Handle authentication and serve the statistics",
  "main": "server-klaus.js",
  "scripts": {
    "start": "node server-klaus.js",
    "build": "cd client-react && npm run build && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max MARIE",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "data.js": "^0.12.8",
    "express": "^4.18.2",
    "stream-interceptor": "^0.1.1",
    "vue-select": "^3.2.0"
  }
}
